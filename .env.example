# Docker Compose Configuration (Production/Docker)
# These variables are used by docker-compose.yml to configure the services.
# Replace with your server's public IP or domain name
SERVER_IP=localhost

# Database Credentials
POSTGRES_USER=praetor
POSTGRES_PASSWORD=praetor
POSTGRES_DB=praetor

# Application Secrets
JWT_SECRET=praetor-secret-key-change-in-production

# SSL Configuration
SSL_ENABLED=true
SSL_DOMAIN=praetor-backend

